[project]
name = "amesh"
version = "0.1.0"
description = "Implementation of A-MESH"
authors = [{name = "Leonardo Veiga Acioly Filho"}]
requires-python = ">=3.11,<3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"